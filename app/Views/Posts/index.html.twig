{% extends "base.html.twig" %}

{% block title %} Posts {% endblock %}

{% block body %}
    <h1>Posts</h1>
    <ul>
        {% for post in posts %}
            <li><h2>{{ post.title }}</h2></li>
            <li>{{ post.content }}</li>
        {% endfor %}
    </ul>

{% endblock %}